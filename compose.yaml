version: "3"
services:
#  app:
#   TODO
  postgres:
    image:
      postgres:14-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: mydatabase
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: secret
      PGDATA: /tmp